# RemindMe MVP 開發任務清單

> 本文件由 `todo.md` 的已定案需求整理而成；不回寫原需求檔。

## 0. 範圍與里程碑

- MVP 目標：在 `macOS` 上完成可本地部署的提醒管理系統。
- 技術基線：`Next.js + NestJS + PostgreSQL + Docker + Swagger UI`。
- 關鍵能力：
  - 任務建立/編輯/刪除/啟用停用
  - 重複提醒 UI 規則（每月幾號 / 每週週幾 / 每日幾時幾分）
  - 桌面彈窗（獨立視窗）、snooze、多提醒堆疊與展開
  - 關頁後仍可提醒（需背景服務）

---

## 1. 後端任務（NestJS）

### 1.1 專案基礎
- [ ] 建立 NestJS 專案模組：`auth(optional placeholder) / reminders / display-settings / scheduler / assets`
- [ ] 接入 PostgreSQL（ORM 任選，但需 migration 能力）
- [ ] 建立 Swagger UI 文件與 DTO 驗證規則

### 1.2 資料模型（最小集）
- [ ] `reminders`
  - `id`, `title`, `content`, `enabled`
  - `schedule_type`（one_time / recurring）
  - `one_time_at`（nullable）
  - `auto_close_enabled`, `auto_close_seconds`（每任務獨立）
  - `snooze_default_seconds`（預設 300，最大 600）
  - `sort_order`（手動排序用）
  - `end_at`（nullable）, `max_occurrences`（nullable）
  - `created_at`, `updated_at`
- [ ] `recurrence_rules`
  - `reminder_id`
  - `rule_mode`（monthly_day / weekly_day / daily_time）
  - `month_day`（1~31, nullable）
  - `week_day`（0~6, nullable）
  - `time_of_day`（HH:mm）
- [ ] `display_settings`（全域單筆）
  - 尺寸、主題、位置角落、目標螢幕 id、是否顯示內容
  - 吉祥物模式（system/custom）、圖片上限與清單
- [ ] `mascot_images`
  - `id`, `path`, `mime`, `width`, `height`, `size_bytes`

### 1.3 API 能力
- [ ] 提醒 CRUD API（含啟用/停用）
- [ ] 排序 API（批次更新 `sort_order`）
- [ ] 重複規則 API（僅允許 3 種 UI 規則）
- [ ] 顯示設定 API（全域）
- [ ] 吉祥物上傳 API（最多 5 張，`jpg/png/svg`，寬高 <= 600）
- [ ] 彈窗事件 API（close / snooze）

### 1.4 商業規則
- [ ] 若規則為「每月 29~31 號」，建立或更新時回傳 `confirm_required` 訊息（前端需二次確認）
- [ ] 短月無該日期時：該月份直接跳過，不補發
- [ ] 同任務未關閉時遇下一輪提醒：新增新彈窗實例，不合併
- [ ] 不保留未讀歷史清單（MVP）

### 1.5 排程與觸發
- [ ] 建立排程引擎（每分鐘 tick 或 next-fire-time 機制）
- [ ] 支援 one-time + recurring 計算下一次觸發時間
- [ ] 支援 `end_at` / `max_occurrences` 停止條件
- [ ] 觸發後透過本機通道通知桌面彈窗服務

---

## 2. 前端任務（Next.js 管理介面）

### 2.1 版面與導覽
- [ ] 左 30% / 右 70% 兩欄布局
- [ ] 左側功能：`顯示工具`、`任務工具`
- [ ] 右側為設定表單 + 即時預覽區

### 2.2 顯示工具頁
- [ ] 尺寸（大/中/小）、主題（明/暗）、角落位置設定
- [ ] 全域目標螢幕選擇器
- [ ] 是否顯示內容切換
- [ ] 吉祥物設定（系統 emoji / 自訂圖片）
- [ ] 上傳前檢查格式與尺寸，超限即時提示
- [ ] 設定變更即時預覽（右下角假彈窗）

### 2.3 任務工具頁
- [ ] 任務清單（新增/編輯/刪除/啟用停用）
- [ ] 拖曳排序並儲存 `sort_order`
- [ ] 排程 UI 組合器：
  - 每月幾號
  - 每週週幾
  - 每日幾時幾分
- [ ] 結束日期、最大提醒次數設定
- [ ] 每任務自動關閉設定（開關 + 時長）
- [ ] 若選每月 `29~31` 號，送出前顯示確認提示

### 2.4 彈窗互動
- [ ] 每則彈窗兩按鈕：左 `稍後提醒`、右 `關閉`
- [ ] snooze 預設 5 分鐘，可調整但最大 10 分鐘
- [ ] 長內容預設截斷，點擊展開；超高區塊可捲動
- [ ] 多提醒堆疊：新提醒在上；超過 1 則僅顯示最高優先，提供展開按鈕與捲軸

---

## 3. 桌面彈窗與背景服務（macOS 先行）

- [ ] 建立桌面容器方案（Electron 或 Tauri 二擇一）
- [ ] 實作背景常駐程序，確保關閉管理頁後仍可觸發提醒
- [ ] 實作獨立小角落彈窗視窗（非瀏覽器通知）
- [ ] 支援指定螢幕顯示（全域目標螢幕）
- [ ] 與後端排程事件串接（本機 IPC / WebSocket 任選）

---

## 4. DevOps / Docker

- [ ] 撰寫 `docker-compose`（web/api/db）
- [ ] 提供初始化流程（DB migration + seed optional）
- [ ] 提供 `.env.example`
- [ ] 補上本地啟動文件（README：一鍵啟動、重建、資料清理）

---

## 5. 測試與驗收清單

## 5.1 核心功能驗收
- [ ] 可建立單次提醒並在指定時間彈窗
- [ ] 可建立 3 種重複規則並正常觸發
- [ ] 每任務可獨立設定自動關閉時長
- [ ] snooze 可延後提醒且上限生效（<=10 分鐘）
- [ ] 同任務未關閉時，下一次觸發會新增彈窗
- [ ] 多提醒可展開/收合，超出可捲動

### 5.2 邊界情境驗收
- [ ] 每月 31 號規則在 2 月會跳過
- [ ] 建立每月 29~31 號時會顯示確認提示
- [ ] 圖片上傳格式/尺寸超限會被阻擋
- [ ] 關閉管理頁後仍可觸發提醒

### 5.3 回歸驗收
- [ ] 重啟後設定與任務仍保留
- [ ] 停用任務不觸發提醒
- [ ] 過期任務樣式正確（灰底）

---

## 6. 建議開發順序（兩週示意）

- 第 1-2 天：資料模型、CRUD API、Swagger
- 第 3-4 天：排程引擎 + 觸發事件
- 第 5-6 天：任務工具頁 + 排程 UI + 拖曳排序
- 第 7-8 天：顯示工具頁 + 吉祥物上傳 + 即時預覽
- 第 9-10 天：桌面彈窗服務 + 背景常駐 + 多螢幕
- 第 11-12 天：整合測試、邊界情境修正、文件補齊

---

## 7. 非 MVP（先記錄，不實作）

- 跨平台安裝包（Windows / Linux）
- 匯出/匯入（備份還原）功能（目標 v2.0）
- 標籤 / 分類 / 附件欄位

---

## 8. 近期已完成實作（前端）

- [x] 主版面調整為左 25% 側欄 / 右 75% 內容區，右側依工具切換內容。
- [x] 側欄工具按鈕產品化（active 狀態、hover 動畫、現代化卡片風格）。
- [x] 顯示工具頁：
  - [x] 顯示內容切換區塊修正跑版。
  - [x] 設定儲存提示改為右上角浮動 toast，成功/失敗顏色區分，並自動消失。
  - [x] 未儲存設定切換工具後回復已儲存值（避免草稿殘留）。
- [x] 預覽彈窗改為真正視窗角落 portal 預覽（固定於整頁四角，不受內容區塊限制）。
  - [x] 半透明顯示，hover 時提高不透明度。
  - [x] 補齊互動按鈕：稍後提醒 / 關閉提醒 / 右上角關閉（視為稍後提醒）。
- [x] 任務工具流程改為「群組列表 -> 提醒項目 -> 編輯頁」。
  - [x] 群組可新增、改名、刪除。
  - [x] 群組內可新增提醒。
  - [x] 提醒項目可編輯、刪除。
  - [x] 麵包屑可點擊返回上層。
- [x] 提醒優先順序改為主列表拖曳排序（以拖曳把手觸發排序操作）。
- [x] Hydration 問題修正：
  - [x] 移除 `useState` 初始化中的 `typeof window !== 'undefined'` 分支讀取。
  - [x] 改為穩定初始狀態 + 客戶端 effect 同步 localStorage，避免 SSR/CSR 首次 HTML 不一致。
